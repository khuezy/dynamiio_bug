// loading.tsx seems to be the culprit - not sure if directly or indirectly. 
// If you remove or rename this file, the error doesn't reproduce.
export default function Loading() {
  return <div>...</div>
}